import 'package:flutter/material.dart';

/// Placeholder — la implementación real (formulario, llamada a
/// POST /api/v1/login, guardado del token) es la tarea "Auth" del plan.
class LoginScreen extends StatelessWidget {
  const LoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('FlutterPuklla — login pendiente')),
    );
  }
}
